x-prest-env: &prest-env
  PREST_MIGRATIONS: /workspace/testdata/migrations
  PREST_QUERIES_LOCATION: /workspace/testdata/queries
  PREST_PG_HOST: postgres
  PREST_PG_HOST_B: postgres-b
  PREST_PG_PORT: "5432"
  PREST_PG_USER: postgres
  PREST_PG_PASS: postgres
  PREST_PG_DATABASE: prest-test
  DATABASE_URL: postgres://postgres:postgres@postgres:5432/prest-test?sslmode=disable
  PGPASSWORD: postgres
  GOTOOLCHAIN: auto
  GOPROXY: https://proxy.golang.org,direct

x-prestd-service: &prestd-service
  image: golang:1.26-bookworm
  working_dir: /workspace
  volumes:
    - ./:/workspace

x-prestd-python-service: &prestd-python-service
  image: prest-python-contract:local
  working_dir: /app
  volumes:
    - ./testdata:/workspace/testdata:ro

services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data

  postgres-b:
    image: postgres:16
    environment:
      - POSTGRES_DB=secondary-cluster
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data

  db-init:
    <<: *prestd-service
    depends_on:
      postgres:
        condition: service_healthy
      postgres-b:
        condition: service_healthy
    environment:
      <<: *prest-env
    command: >-
      bash -lc "apt-get update && apt-get install -y --no-install-recommends postgresql-client git ca-certificates && bash -x ./testdata/db-init.sh"
    restart: "no"

  prestd:
    <<: *prestd-service
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_ACCESS_RESTRICT: "false"
      PREST_PG_SINGLE: "false"
      PREST_HTTP_PORT: "3000"
    command: >-
      bash -lc "export PATH=/usr/local/go/bin:/go/bin:$$PATH && apt-get update && apt-get install -y --no-install-recommends curl ca-certificates && go run ./cmd/prestd/main.go"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:3000/_health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s

  prestd-multicluster:
    <<: *prestd-service
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      postgres-b:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest_multicluster.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_HTTP_PORT: "3001"
    command: >-
      bash -lc "export PATH=/usr/local/go/bin:/go/bin:$$PATH && apt-get update && apt-get install -y --no-install-recommends curl ca-certificates && go run ./cmd/prestd/main.go"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:3001/_health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s

  prestd-auth:
    <<: *prestd-service
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_AUTH_ENABLED: "true"
      PREST_JWT_KEY: integration-test-secret
      PREST_PG_SINGLE: "false"
      PREST_HTTP_PORT: "3002"
    command: >-
      bash -lc "export PATH=/usr/local/go/bin:/go/bin:$$PATH && apt-get update && apt-get install -y --no-install-recommends curl ca-certificates && go run ./cmd/prestd/main.go"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:3002/_health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s

  prestd-python:
    <<: *prestd-python-service
    build:
      context: .
      dockerfile: Dockerfile.python
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_ACCESS_RESTRICT: "false"
      PREST_AUTH_ENABLED: "false"
      PREST_PG_SINGLE: "false"
      PREST_HTTP_PORT: "3000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3000/_health', timeout=2).read()"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  prestd-python-multicluster:
    <<: *prestd-python-service
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      postgres-b:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest_multicluster.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_ACCESS_RESTRICT: "false"
      PREST_AUTH_ENABLED: "false"
      PREST_HTTP_PORT: "3000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3000/_health', timeout=2).read()"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  prestd-python-auth:
    <<: *prestd-python-service
    depends_on:
      db-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest.toml
      PREST_DEBUG: "true"
      PREST_JWT_DEFAULT: "false"
      PREST_CACHE_ENABLED: "false"
      PREST_AUTH_ENABLED: "true"
      PREST_JWT_KEY: integration-test-secret
      PREST_PG_SINGLE: "false"
      PREST_HTTP_PORT: "3000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:3000/_health', timeout=2).read()"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s

  tests:
    <<: *prestd-service
    depends_on:
      prestd:
        condition: service_healthy
      prestd-multicluster:
        condition: service_healthy
      prestd-auth:
        condition: service_healthy
    environment:
      <<: *prest-env
      PREST_CONF: /workspace/testdata/prest.toml
      PREST_TEST_URL: http://prestd:3000
      PREST_MULTICLUSTER_TEST_URL: http://prestd-multicluster:3001
      PREST_AUTH_TEST_URL: http://prestd-auth:3002
    command: >-
      bash -lc "apt-get update && apt-get install -y --no-install-recommends postgresql-client git ca-certificates && bash -x ./testdata/runtest.sh"

  contract-tests:
    image: python:3.13-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    depends_on:
      prestd:
        condition: service_healthy
      prestd-multicluster:
        condition: service_healthy
      prestd-auth:
        condition: service_healthy
    environment:
      PREST_TEST_URL: http://prestd:3000
      PREST_MULTICLUSTER_TEST_URL: http://prestd-multicluster:3001
      PREST_AUTH_TEST_URL: http://prestd-auth:3002
      CONTRACT_ARGS: ""
    command: >-
      bash -lc "python -m pip install -r tests/contract/requirements.txt && python -m pytest tests/contract --target=go $${CONTRACT_ARGS}"

  contract-tests-python:
    image: python:3.13-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    depends_on:
      prestd-python:
        condition: service_healthy
      prestd-python-multicluster:
        condition: service_healthy
      prestd-python-auth:
        condition: service_healthy
    environment:
      PY_PREST_TEST_URL: http://prestd-python:3000
      PY_PREST_MULTICLUSTER_TEST_URL: http://prestd-python-multicluster:3000
      PY_PREST_AUTH_TEST_URL: http://prestd-python-auth:3000
      CONTRACT_ARGS: ""
    command: >-
      bash -lc "python -m pip install -r tests/contract/requirements.txt && python -m pytest tests/contract --target=python $${CONTRACT_ARGS}"
